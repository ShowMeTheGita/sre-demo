# "all" is available as a host by default to target all servers, including ansible

[ansible]
localhost ansible_connection=local

[monitoring_servers]
grafana ansible_host=grafana
prometheus ansible_host=prometheus
alertmanager ansible_host=alertmanager

[app_servers]
webapp ansible_host=webapp

