---
- name: Display IP addresses
  hosts: grafana
  gather_facts: False
  
  tasks:
    - name: Run simple shell command
      shell: "whoami"
      register: whoishe
    
    - debug:
        msg: "{{ whoishe.stdout }}"
